# TODO: Dev 2 — Extract hand landmarks from ASL dataset images using MediaPipe
# Input: data/raw/{A,B,C,...Z}/*.jpg
# Output: data/processed/landmarks.csv (columns: label, x0, y0, z0, x1, y1, z1, ..., x20, y20, z20)
print("collect_landmarks.py — stub")
